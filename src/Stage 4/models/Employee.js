export default class Employee {
  // constructor
  constructor (id, name, phone, title) {
    super(); 
    this.id = id;
    this.name = name;
    this.phone = phone;
    this.title = title;
  }
  
  // updateName
  updateName(name){
      name: this.name
  }
  // updatePhone
  updatePhone(phone) {
    phone: this.phone

  }
   // updateTitle
   updateTitle(title) {
    title: this.title
   }
}